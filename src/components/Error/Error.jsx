import React, { Component } from "react";

class Error extends Component {
  render() {
    return (
      <div className="error">
        <h1>
          Error
        </h1>
        <p>Aha, so this is where the Error page content lives.</p>
      </div>
    );
  }
}

export default Error;
